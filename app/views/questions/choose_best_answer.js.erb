<% if @previous_best_answer.present? %>
  $('#answer-best-<%= @previous_best_answer.id %>').hide()
<% end %>

$('#answer-best-<%= question.best_answer.id %>').show()
$('#answer-<%= question.best_answer.id %>').prependTo('.answers')
