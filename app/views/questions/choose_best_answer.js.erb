<% if @previous_best_answer.present? %>
  $('#answer-<%= @previous_best_answer.id %> .best-answer').hide()
<% end %>

$('#answer-<%= question.best_answer.id %> .best-answer').show()
$('#answer-<%= question.best_answer.id %>').prependTo('.answers')
